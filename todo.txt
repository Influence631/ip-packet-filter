would be good :

"cover" - i dont like the machinery verilator needs around this
symbyosys - format verification of the skid buffer

1. i want to make an SVA file and add assertions to the fifo 
+ replace the assetions in the skid buffer with macros
2. check the resources usage of the fifo when synthesized -> see how to infer lutram / bram.

2. Skid buffer - smallest complete AXI-Stream block. 
3. Sync FIFO - adds pointers/full-empty on top of a handshake
4. AXI-Lite register file
5. Filter datapath - store-and-forward decide at tlast, release or discard.
6. scapy stimulus + reference model

property assertions
avalon mm -> axi Lite
avalon -> axi in endian swapper 